# =============================================================================
# Pleasy - Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values:
#   cp .env.example .env
# =============================================================================

# Database
PGDATABASE=pleasy
PGUSER=postgres
PGPASSWORD=change_me_to_a_strong_password

# JWT Secret (generate with: openssl rand -hex 64)
JWT_SECRET=change_me_to_a_random_string

# App port (the port you'll open in the browser)
APP_PORT=80

# Database external port (optional, for connecting with a DB client)
DB_EXTERNAL_PORT=5432

# CORS - Comma-separated list of allowed origins (optional)
# Only needed if frontend and backend are on different domains
# CORS_ORIGINS=https://app.yourdomain.com,https://yourdomain.com
