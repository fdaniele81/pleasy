name: Build, push and deploy backend (Staging)

on:
  push:
    branches: ["develop"]
    paths:
      - "backend/**"
      - "version.json"
      - ".github/workflows/deploy-backend-staging.yml"

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: fdaniele81/pleasy-backend

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      actions: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: backend/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:develop
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}

  deploy:
    name: Deploy to VPS (Staging)
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: Staging

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup SSH key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SERVER_SSH_KEY }}

      - name: Deploy to VPS
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} << 'EOF'
          cd /srv/pleasy
          docker compose pull pleasy_backend_staging
          docker compose up -d --force-recreate --no-deps pleasy_backend_staging
          docker exec pleasy_backend_staging npm run migrate
          docker system prune -f
          EOF
